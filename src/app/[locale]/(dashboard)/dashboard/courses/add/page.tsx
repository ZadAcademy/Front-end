import AddCoursePage from '@/features/dashboard/courses/add-course-page';

/**
 * /dashboard/courses/add — Add a new course in the dashboard.
 * Thin wrapper that renders the feature component.
 */
export default function AddCourseRoute() {
  return <AddCoursePage />;
}
