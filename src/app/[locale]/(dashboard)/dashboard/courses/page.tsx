import ListCoursesPage from '@/features/dashboard/courses/list-courses-page';

/**
 * /dashboard/courses — List all courses in the dashboard.
 * Thin wrapper that renders the feature component.
 */
export default function ListCoursesRoute() {
  return <ListCoursesPage />;
}
