import DashboardHomePage from "@/features/dashboard/dashboard-home-page";

/**
 * /dashboard — Dashboard home route.
 * Renders the dashboard overview page.
 */
export default function DashboardRoute() {
  return <DashboardHomePage />;
}
