import LandingPage from "@/features/landing-page/landing-page"

/**
 * IndexPage — The main entry page for each locale.
 * Renders the full landing page experience.
 */
export default function IndexPage() {
  return <LandingPage />;
}
