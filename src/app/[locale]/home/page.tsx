import HomePage from '@/features/home/home-page';

/**
 * /home route — Authenticated home page.
 * Renders the HomePage feature component which includes:
 * courses grid, filter panel, and pagination.
 */
export default function HomeRoute() {
  return <HomePage />;
}